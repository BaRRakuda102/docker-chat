version: '3.8'

services:
  chat:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - uploads_data:/app/uploads
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  uploads_data: